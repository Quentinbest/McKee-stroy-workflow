# Pi Adapter

Pi uses root `AGENTS.md` and generated shared skills under `.agents/skills/`.
No extension is required or approved for baseline operation. Run repository
verification commands directly from the project root.
