# 麦基故事工作流 — 用户手册

> English version: [MANUAL.md](MANUAL.md)

**版本**：2.0（V1 + V2 + V3 核心）
**最后更新**：2026-05-21

---

## 目录

1. [这是什么](#1-这是什么)
2. [五个核心难题](#2-五个核心难题)
3. [安装配置](#3-安装配置)
4. [架构概览](#4-架构概览)
5. [项目生命周期](#5-项目生命周期)
6. [快速上手：从种子到第一个场景](#6-快速上手从种子到第一个场景)
7. [工作流技能参考手册](#7-工作流技能参考手册)
8. [方法论技能参考手册](#8-方法论技能参考手册)
9. [基础设施技能参考手册](#9-基础设施技能参考手册)
10. [智能代理参考手册](#10-智能代理参考手册)
11. [模板参考手册](#11-模板参考手册)
12. [完整工作流演示](#12-完整工作流演示)
13. [进阶功能：V3](#13-进阶功能v3)
14. [故障排查](#14-故障排查)

---

## 1. 这是什么

这是一个**以麦基方法论为核心的故事生成平台**——结合了 Claude Code 技能（Skills）与专用智能代理（Agents），将作者从一个模糊的创作冲动，引导至结构严谨、文字打磨的完整散文草稿。

系统基于罗伯特·麦基（Robert McKee）的《故事》（*Story*, 1997）——迄今为止最为严格的故事结构、人物塑造、类型理论与主题意义的综合框架。系统输出的每一个成果都扎根于麦基方法论：每个场景必须发生转折（转折点，Turning Point），每个危机必须是真正的两难困境（Dilemma），每个主控思想（Controlling Idea）必须通过结构来戏剧化呈现，而非通过对话来宣讲。

**它不是什么**：基于提示词（Prompt）的故事生成器。系统运行的是迭代工作流，跟踪项目状态，召唤具有全新视角的批评代理，并执行收敛协议。它是一个**用于严肃创作的平台**，而非文字补全工具。

### 麦基框架能提供什么

麦基框架将优秀故事讲述的**结构维度**转化为可执行的规则：
- 每个场景都有一个价值取向（Value Charge），且必须发生转变——场景必须"转"
- 每个故事都有一条脊椎（Spine）：激励事件（Inciting Incident）→ 逐步并发（Progressive Complications）→ 危机（Crisis）→ 高潮（Climax）→ 结局（Resolution）
- 危机是真正的两难困境——不可调和的正反价值，或无法回避的必要之恶
- 高潮因果地流向危机的决策，而非源于巧合
- 主控思想是每个场景最终服务的那一句"价值 + 原因"

麦基**假设**作家自带其余的东西：品味、声音、真实、潜文本（Subtext）、必然性中的惊喜（Inevitable-Surprise）。本平台提供的，正是麦基所假设作家已具备的那些东西。

---

## 2. 五个核心难题

系统围绕五个维度设计——这五个维度是结构合规性无法独立解决的：

| 难题 | 为何困难 | 本系统的应对方式 |
|---|---|---|
| **品味** | "好"是被认出的，而非被规定的；AI默认输出趋向平均 | 批评代理矩阵 + 止损收敛协议 |
| **具体性** | AI默认使用通用语言："一个男人走进了房间" | `/mck-specificity-forge` + 世界圣经 + `specificity-auditor` 代理 |
| **潜文本** | 训练数据中充满直白对话 | `/mck-subtext-5layer` 五层创作模型：写任何文字之前先填写五层 |
| **必然性中的惊喜** | 需要从开篇就植入支持双重解读的数据 | `/mck-surprise-plant` + `surprise-auditor` 代理 |
| **诚实性** | 系统没有活过、没有历史 | `/mck-honesty` 诚实引擎 + 作者人格（Author Persona）中的真相图书馆 |

---

## 3. 安装配置

### 前提条件

- 已安装 Claude Code CLI（`claude` 命令可用）
- Git
- 一个存放故事的项目目录

### 全局安装技能

安装到 `~/.claude/skills/` 的技能在所有 Claude Code 会话中均可使用。

```bash
# 克隆仓库
git clone https://github.com/Quentinbest/McKee-stroy-workflow.git

# 全局安装所有技能
cp -r McKee-stroy-workflow/skills/* ~/.claude/skills/
```

### 将代理安装到故事项目中

代理（Agents）放入故事项目目录的 `.claude/agents/` 文件夹：

```bash
# 在你的故事项目目录中执行：
mkdir -p .claude/agents/
cp -r McKee-stroy-workflow/agents/* .claude/agents/
```

### 初始化新项目

使用提供的模板初始化项目：

```bash
# 创建项目结构
mkdir -p drafts/my-story/{characters,scenes,prose,01-act-1,02-act-2,03-act-3}

# 复制模板
cp McKee-stroy-workflow/templates/lifecycle.json drafts/my-story/lifecycle.json
cp McKee-stroy-workflow/templates/state.json drafts/my-story/state.json
```

或者，直接在 Claude Code 会话中运行：

```
/story-new "你的创作种子"
```

技能会自动初始化目录结构。

### 验证安装

在任意 Claude Code 会话中运行：

```
/story-status
```

如果技能加载成功并返回报告（即便显示"未找到项目"），则安装成功。

---

## 4. 架构概览

平台分为四个层级：

```
┌──────────────────────────────────────────────────────────────────────┐
│  第一层 — 工作流技能  (/story-*)                                      │
│  面向用户的入口点。在主对话上下文中运行。可见、可迭代、可打断。          │
│  story-new · story-status · story-premise · story-spine · story-cast │
│  story-act · story-scene · story-audit · story-revise · story-publish│
├──────────────────────────────────────────────────────────────────────┤
│  第二层 — 方法论技能  (/mck-*)                                        │
│  麦基方法论的具体实现。可复用、可组合、可单独调用。                      │
│  V1: subtext-5layer · controlling-idea · beat-to-prose · crisis-     │
│      dilemma · arc-walk · gap-find                                   │
│  V2: image-thread · setup-payoff · specificity-forge · voice-first · │
│      exposition-ammo · negation-of-negation                          │
│  V3: honesty · surprise-plant                                        │
├──────────────────────────────────────────────────────────────────────┤
│  第三层 — 生成代理  （产出成果物）                                      │
│  有界、隔离、冷启动。用于复杂的生成任务。                               │
│  premise-prospector · character-forger · structure-skeleton ·        │
│  scene-architect · beat-miner · prose-drafter                        │
├──────────────────────────────────────────────────────────────────────┤
│  第四层 — 批评代理  （以全新视角审阅）                                  │
│  盲读草稿并返回发现。对抗性设计，不带作者偏见。                         │
│  cliche-hunter · antagonism-stress-tester · crisis-climax-auditor ·  │
│  subtext-whisperer · composition-conductor · continuity-supervisor · │
│  voice-drift-detector · specificity-auditor · reader-simulator ·     │
│  pacing-analyst · surprise-auditor · tournament-judge                │
└──────────────────────────────────────────────────────────────────────┘
    底层支撑：lifecycle.json · state.json · 世界圣经 ·
    persona.md · voice-anchors.md · misdirection-plan.md
```

### 为什么不把所有东西都做成代理？

代理是冷启动的——每次调用都要从零重新推导所有上下文。对于迭代性、对话性的工作（锻造主控思想、走查人物弧光、创作潜文本），这种冷启动开销是浪费的。技能在主对话上下文中运行：方法论可见，用户可以随时介入，会话中已加载的上下文直接可用。

代理被保留用于冷启动恰好是优势的三种情形：
1. **上下文隔离**——批评者必须在没有作者偏见的情况下阅读作品
2. **并行计算**——同时召唤4个批评者
3. **体量**——会消耗父级上下文窗口的任务（例如 prose-drafter 处理长场景）

---

## 5. 项目生命周期

每个项目按以下状态推进，由 `lifecycle.json` 跟踪：

```
inspiration（灵感）
    ↓  /story-new
premise_locked（前提锁定）
    ↓  /mck-controlling-idea 或 /story-premise
genre_locked（类型锁定）
    ↓  /genre-cartographer（代理）或手动
controlling_idea_locked（主控思想锁定）
    ↓  /story-spine
setting_locked（世界设定锁定）
    ↓  /story-cast
cast_locked（人物阵容锁定）
    ↓  /story-spine
spine_locked（故事脊椎锁定）
    ↓  /story-act（每幕重复）
act_design_locked（幕结构锁定）
    ↓  /story-scene（每个场景重复）
scene_cards_locked（场景卡锁定）
    ↓  /story-scene（散文模式，每个场景重复）
prose_drafted（散文草稿完成）
    ↓  /story-audit
critic_passed（批评审核通过）
    ↓  /story-revise
polished（润色完成）
    ↓  /story-publish
done（完成）
```

每个关卡都有**必须通过的断言**（Must-pass Predicates）。如果断言未通过，系统不会推进项目——它会明确指出具体的失败项并提供修复路径。

---

## 6. 快速上手：从种子到第一个场景

以下演示如何在单次会话中从零开始，抵达第一个场景草稿。

### 第1步 — 启动新项目

```
/story-new "一位修仙宗门的长老，在多年前用了异端方法，却最终功成名就"
```

技能会问："这个种子有什么让你放不下？"——那个**萦绕**（haunt）。

你回答，技能提炼出情感核心，推导项目代号（slug），初始化目录结构，并提供锻造作者人格的选项（推荐）。

随后召唤 `premise-prospector` 代理，生成5个前提候选。

### 第2步 — 锁定前提

查看5个候选，选择一个，或要求变体。技能写入 `premise-card.md`，推进生命周期。

### 第3步 — 锻造主控思想

```
/controlling-idea-architect FORGE
```

生成3个候选（理想主义者/悲观主义者/讽刺性）。你选定后，诚实引擎自动运行：这个主控思想是扎根于真实观察的，还是道德口号？

### 第4步 — 构建故事脊椎

```
/story-spine
```

召唤 `structure-skeleton` 代理。技能对返回的脊椎进行麦基断言审核（激励事件、危机两难、高潮因果链），迭代最多3轮。锁定后，提供设计必然性惊喜架构的选项。

### 第5步 — 设计人物阵容

```
/story-cast
```

召唤 `character-forger` 创建主角及核心人物文件。召唤 `cast-balancer` 验证每个人物对主角施加的压力唯一且不可重叠。

### 第6步 — 规划第一幕

```
/story-act 1
```

生成第一幕所有场景的场景卡（Scene Cards）：目标、冲突、转折点、价值取向转变。

### 第7步 — 写作场景1.1

```
/story-scene 1.1
```

加载场景卡、人物文件、世界圣经和作者人格。逐层填写五层潜文本模型。逐节拍草拟散文。并行运行3个批评代理（cliche-hunter、subtext-whisperer、continuity-supervisor）。应用修订。提交散文。

从种子到散文，7步完成。

---

## 7. 工作流技能参考手册

### `/story-new`

**用途**：从任何种子启动新故事项目。
**触发词**：`/story-new`，"开始新故事"，"我有个想法"，"新项目"
**输入**：种子（图像、梦境、新闻剪报、随听随记的一行话、情绪、假设性问题）
**输出**：初始化的项目目录 + `premise-card.md` + `lifecycle.json`

**执行步骤**：
1. 提取**萦绕**——无法放下的情感核心
2. 从种子推导项目代号
3. 创建目录结构（`characters/`、`scenes/`、`prose/`、各类模板文件）
4. 提供锻造作者人格的选项（步骤3.5）
5. 召唤 `premise-prospector` 生成5个前提候选
6. 引导前提选择并锁定

**何时使用**：永远第一步。所有项目从这里开始。

---

### `/story-status`

**用途**：仪表盘——当前生命周期状态、已锁定关卡、成果物清单、建议下一步。
**触发词**：`/story-status`，"我现在在哪"，"什么锁定了"
**输入**：无（自动读取 lifecycle.json）
**输出**：对话中的状态报告

**显示内容**：
- 生命周期关卡及 ✅/⬜ 状态
- 成果物数量（人物文件、场景卡、散文文件）
- 基于当前状态的建议下一步行动

**何时使用**：每次会话开始时定向，或不确定下一步时查阅。

---

### `/story-premise`

**用途**：单独生成并锁定前提卡。
**触发词**：`/story-premise`，"生成前提"，"前提候选"
**输入**：萦绕内容、类型偏好、已知约束
**输出**：`drafts/{slug}/premise-card.md`

**何时使用**：不通过 story-new 启动项目时，或需要在新约束下重新生成前提候选时。

---

### `/story-spine`

**用途**：构建故事的承重骨架——从激励事件到危机、高潮和结局。
**触发词**：`/story-spine`，"构建脊椎"，"故事结构"，"规划情节"
**输入**：`premise-card.md`，`controlling-idea.md`（如已锁定），`genre-contract.md`（如已锁定）
**输出**：`drafts/{slug}/spine.md`（含美人鱼时间线 + 主要戏剧性问题陈述）

**检验的断言**：
- 激励事件打破主角的生活平衡，引发主要戏剧性问题（MDQ）
- 每个并发比上一个更严峻；至少一个逆转主角策略；至少一个是不可逆转的
- 危机是真正的两难困境（不是"有明显正确答案的艰难选择"）
- 高潮因果地来自危机决策
- 结局展示价值变化后的新均衡

**锁定后**：提供 `/mck-surprise-plant DESIGN` 选项——在写第一幕之前设计必然性惊喜架构。

**何时使用**：主控思想锁定后。脊椎是最重要的结构成果物。

---

### `/story-cast`

**用途**：将完整人物阵容设计为作用于主角的压力系统。
**触发词**：`/story-cast`，"设计人物"，"人物系统"
**输入**：`premise-card.md`，`spine.md`，`genre-contract.md`
**输出**：`drafts/{slug}/characters/` 中的人物文件 + `cast-design.md`

**每个人物产出内容**：
- 人物塑造（Characterization，外表——年龄、外貌、举止）
- 真实性格（True Character，极限压力下揭露的——创伤、欲望、恐惧）
- 人物维度（Dimension，至少3个矛盾点）
- 这个人物对主角施加的独特压力

**冗余性审核**：召唤 `cast-balancer` 验证没有两个人物施加相同的压力。推荐合并、删除或提升的方案。

**何时使用**：脊椎锁定后，第一幕场景规划开始前。

---

### `/story-act`

**用途**：规划单幕的场景序列——每个场景的目标、冲突、转折点和价值取向转变。
**触发词**：`/story-act 1`，`/story-act 2`，"规划第一幕"，"场景序列"
**输入**：`spine.md`，幕结构设计，人物文件
**输出**：`drafts/{slug}/scenes/{幕}-{场景}.md` 中的场景卡

**执行步骤**：
1. 召唤 `act-designer` 确定幕结构和序列节奏
2. 召唤 `scene-architect` 为每个场景制作场景卡
3. 审核每张卡：场景有转折吗？转折点是由决策引发的，而非巧合？
4. 依据该幕在 `target_total` 中的占比，为每张场景卡分配 `length_budget`（承重场景重、过渡场景轻）——这是后续密度检查的基准
5. **密度检查**（散文已存在时）：将每个场景的实际字数与预算对比——标记字数不足（低于预算 60% 且无 `length_note` 说明）、密度倒挂（承重场景比其周围的过渡场景更薄）以及整幕低于预算
6. **搭建 `state.json`**（地理/专名锁、人物位置与认知）——在散文起草前完成，不再可选；缺失时 `/story-scene` 将以降级模式运行

**何时使用**：逐幕执行，脊椎锁定后。不要一次规划所有幕——第一幕草稿揭示的内容应当纳入第二幕设计。

---

### `/story-scene`

**用途**：草拟或修改单个场景——平台中使用最频繁的技能。
**触发词**：`/story-scene 1.1`，"写场景2.3"，"草拟下一个场景"，"处理这个场景"
**输入**：指定场景的场景卡
**输出**：`drafts/{slug}/prose/{幕}/{场景}.md` 中的散文文件

**工作流程**（全部在主上下文中——可见且可随时打断）：

1. **定位**——查找或创建场景卡
2. **加载上下文**——场景卡、人物文件、世界圣经、声音锚点、前一场景散文、作者人格工作参考
3. **五层潜文本模型**——每个角色：创伤/欲望/恐惧/战术/文本策略（在写任何散文之前填写；展示给用户确认）
4. **鸿沟识别**——视角角色期望什么？场景实际传递了什么？
5. **逐节拍草拟**——一次一个节拍；每个节拍通过作者人格决策协议过滤（信念/美学/拒绝/真相）；逐节拍展示给用户
6. **转折点检查**——价值取向已开始转变；转折点在行动中，而非在对话中
7. **批评代理审核**——`cliche-hunter` + `subtext-whisperer` + `continuity-supervisor`，按能力阶梯运行（并行代理 → 原生批评工具 → 带"换新眼"重置的上下文串行）。一条自审偏差提醒会标记起草者重新引入作者人格的禁忌手法（点破式心理描写、"行为—破折号—解释"式注解）。每个批评在返回摘要前先把报告写入磁盘。
8. **修订**——应用发现；止损协议每个节拍限3轮
9. **提交**——泄漏扫描（按"散文正文 vs `<!-- AUTHORING -->` 围栏"约定剥离创作标记/节拍注释）→ 写入散文文件 → 更新 state.json → 更新场景卡
10. **提交后一致性检查**——意象系统节奏检查（意象间隔、关键意象状态、新意象检测）+ 铺垫-收束台账检查（悬空铺垫、新铺垫检测、无根之收束）

**起草模式**：默认逐场。当被要求草拟整个序列或整幕时，**批量模式**对每个场景跑完整流程，但仅在序列转折点暂停（或遇到批评的严重问题、回溯触发、地理矛盾时暂停）——在保留各项护栏的同时，免去逐场"继续"的催促。

**回溯协议**：如果场景在某断言上反复失败，技能会诊断上游原因（场景卡/人物欲望-创伤/脊椎事件）并提出具体的修改方案。最多回溯3层。

**何时使用**：主要的场景写作入口。每个场景都用这个技能。

---

### `/story-audit`

**用途**：对草稿运行完整的麦基批评套件——结构合规性、对抗力量平衡、陈词滥调检查、危机/高潮有效性、潜文本、构图、诚实性、惊喜。
**触发词**：`/story-audit`，"审核故事"，"运行批评代理"，"这个草稿哪里有问题"
**输入**：所有散文文件 + 脊椎 + 主控思想 + 类型合同 + 人物文件
**输出**：`drafts/{slug}/audit-report.md`（包含排名发现 + 结构断言表）

**批评代理**（按能力阶梯运行——第 0 步）：
- `antagonism-stress-tester`——对抗力量平衡
- `crisis-climax-auditor`——两难困境真实性，高潮因果性
- `cliche-hunter`——套话、套图、套路
- `subtext-whisperer`——直白对话和铺陈
- `composition-conductor`——铺垫/收束，意象系统，节奏
- `surprise-auditor`——如果存在 `misdirection-plan.md`
- `/mck-honesty TEST`——始终在上下文中运行（需要人格中的真相图书馆）

**能力阶梯（第 0 步）**：选择宿主支持的最高一档——（1）Claude Code 上的并行隔离代理；（2）宿主提供时的原生批评工具（如 Pi 的 `cliche_hunt`/`subtext_check`/`antagonism_test`）；（3）带"换新眼"维度重置的上下文串行。所选档位记入报告抬头。技能内附**跨平台批评映射表**，把每个维度在各档位之间对应起来。

**容错**：每个批评在返回摘要*之前*先写入 `drafts/{slug}/audit/{critic}.md`，因此运行途中遇到会话/限流中断可恢复——重启后读取已落盘的报告，仅重跑缺失的批评。

**新增断言**：报告现包含*长度是否达标*与*密度未倒挂*（承重幕最重），以及一节"长度与配比"，将实际总量/各幕字数对照 `act-design.md` 的预算。

**何时使用**：一幕或全文散文草稿完成后。关卡要求：`critic_passed` 生命周期状态需要所有结构断言通过。

---

### `/story-revise`

**用途**：多轮修订编排者——每次专注一个维度。
**触发词**：`/story-revise`，"修改草稿"，"修订一遍"，"改进写作"
**输入**：`audit-report.md` + 所有散文 + 脊椎

**七轮修订**：

| 轮次 | 修复目标 | 不触碰 |
|---|---|---|
| 第1轮 — 结构 | 没有转折的场景；软弱的幕收尾转折点 | 散文风格 |
| 第2轮 — 陈词滥调 | 套话、套图、套路 | 潜文本、结构 |
| 第3轮 — 潜文本 | 直白对话和铺陈 | 声音、具体性 |
| 第4轮 — 意象系统 | 意象断层、关键意象节奏、铺垫-收束失衡 | 散文节奏 |
| 第5轮 — 声音 | 漂移、风格不一致、时代错误的词汇 | 内容 |
| 第6轮 — 具体性 | 应当具体的通用名词和动词 | 整段落 |
| 第7轮 — 读者模拟 | 节奏跌落、投入度断层、困惑点 | （仅定性，无通过/失败断言） |

每轮修订写入 `revision-log.md`。止损协议适用：同一场景，同一断言，连续3轮失败 → 立即升级处理。

**何时使用**：`/story-audit` 返回发现后。按顺序执行各轮，除非审核建议不同的顺序。

---

### `/story-publish`

**用途**：将所有已提交的散文文件组装成最终稿件。
**触发词**：`/story-publish`，"组装稿件"，"导出故事"
**输入**：`drafts/{slug}/prose/` 中的所有散文文件
**输出**：`drafts/{slug}/manuscript.md`（长篇作品可分文件）

**执行内容**：
1. **泄漏扫描**（组装前）：扫描每个散文正文的创作标记泄漏（`<!-- ... -->`、`Seq N.N`、`本拍`、`✗ 已证伪`、`calendar`/`physical` 年龄标签、frontmatter 渗漏），并在各自的源场景文件中处理。承载读者所需信息的标记（如年龄体系标签）会提交用户做内容决策，而非静默删除。
2. **长度核对**：将实际总量/各幕字数对照 `act-design.md` 预算；在组装*之前*标记字数不足或密度倒挂的草稿——付梓是发现它的最后一道关。
3. 按场景顺序组装散文、统一格式、生成干净稿件。
4. 记录 `manuscript_built_at`，以便 `/story-status` 日后在散文于付梓后被改动时检测**稿件过期**。

**何时使用**：设置 `polished` 生命周期状态后。

**`done` 之后的重新开启**：付梓后的改动（如扩写字数不足的某一幕）是合理的——这会把项目重新打开到 `polished`。稿件、版权页字数、备注都会过期，须重跑 `/story-publish` 重新生成（并刷新 `manuscript_built_at`）。`done` 并非不可更改。

---

## 8. 方法论技能参考手册

这些技能可独立调用，也可由工作流技能在内部调用。它们编码了具体的麦基技法。

---

### `/mck-subtext-5layer`（五层潜文本）

**用途**：以5个层级创作对话和动作——平台中最重要的单个技能。
**触发词**：`/mck-subtext-5layer`，"应用潜文本"，"人物说话太直白了"

**五个层级（在写任何文字之前填写）**：

| 层级 | 问题 | 说明 |
|---|---|---|
| 1. 创伤（Wound） | 此刻这个人物内心活跃的是什么伤？ | 尚未愈合的损伤 |
| 2. 欲望（Want） | 这个场景中这个人物想要什么？（用主动动词） | 意识层面的；可能是错的 |
| 3. 恐惧（Fear） | 他们不敢承认什么？ | 从不大声说出口 |
| 4. 战术（Tactic） | 他们在对另一个人施加什么动词？ | 他们如何试图获得欲望 |
| 5. 文本策略（Text Strategy） | 他们会怎么说话？ | 伪装——不是他们的真意 |

**对话**写在第5层。第1—4层是写任何一个字之前完成的创作工作。文本≈欲望 = 直白。文本≠欲望≠恐惧 = 潜文本。

**何时使用**：在写任何对话密集型场景之前；`/story-scene` 第3步（自动执行）；任何结构正确却感觉"平淡"的场景。

---

### `/mck-controlling-idea`（主控思想）

**用途**：锻造故事的"价值+原因"句——每个场景最终必须服务的那一句话。
**触发词**：`/mck-controlling-idea`，"这个故事的主题是什么"，"主控思想"

**格式**："**[价值终态]** 因为 **[行动中的原因]**。"

示例：
- "爱得以延续，因为它不求回报。"
- "正义腐化，当法官忘记了什么叫做犯错。"
- "自由摧毁那些无法承受自由所揭示之事的人。"

**何时使用**：早期，在脊椎之前。薄弱的主控思想会产出结构合规却无话可说的故事。

---

### `/mck-beat-to-prose`（节拍转散文）

**用途**：将节拍表转化为打磨后的散文——一次一个节拍，应用潜文本和具体性。
**触发词**：`/mck-beat-to-prose`，"从这个节拍表写散文"
**输入**：节拍表（来自 `beat-miner` 代理或作者自写）
**输出**：场景的散文草稿

**何时使用**：在已有节拍表的情况下，作为 `/story-scene` 的独立替代方案。或对于非常短的场景，完整的 `/story-scene` 流程过于繁琐时。

**泄漏与禁忌手法**：`<!-- Beat N -->` 进度注释是脚手架——提交时剥离。所有创作标记（Seq 引用、信念追踪、强度/年龄标签）都不得留在散文正文里（只放 frontmatter 或末尾的 `<!-- AUTHORING -->` 围栏）。失败清单会标记**点破式心理描写**（`他知道…`／`不是X，是Y`）与**破折号注解**（一个行为节拍后接 `——` 解释其含义）——这是起草者最常自行植入的手法；修法是做减法。

---

### `/mck-crisis-dilemma`（危机两难困境）

**用途**：将艰难选择磨砺成真正的两难困境。
**触发词**：`/mck-crisis-dilemma`，"强化危机"，"这个危机不是真正的两难困境"

**关键区分**：
- **艰难选择**：一个选项明显更好；主角犹豫，但正确答案是可见的
- **真正的两难困境**：两个选项都以某种不可替代的东西为代价；主角无法两全；选择揭示真实性格

**四种两难困境结构**：
- 正vs正（两种正当价值无法共存）
- 恶vs恶（必要之恶——你能承受哪种伤害？）
- 自我vs他者（你的生存以他人为代价，或反之）
- 欲望vs需求（意识层欲望 vs 真正需求）

**何时使用**：设计危机时；`/story-audit` 标记"危机是艰难选择而非两难困境"时。

---

### `/mck-arc-walk`（弧光走查）

**用途**：在脊椎事件中映射人物弧光——内心转变或拒绝转变的轨迹。
**触发词**：`/mck-arc-walk`，"映射人物弧光"，"[人物]如何转变"
**输入**：人物文件 + `spine.md`
**输出**：逐节拍弧光表（含启示节点和价值走势图）

**何时使用**：人物文件和脊椎锁定后。在幕规划之前调用，确保弧光的转折点位于正确的幕位置。

---

### `/mck-gap-find`（鸿沟查找）

**用途**：在任何节拍、场景或序列中找到鸿沟（期望vs结果）。
**触发词**：`/mck-gap-find`，"找鸿沟"，"这个场景的鸿沟在哪"

**鸿沟是什么**：人物期望发生之事（其行动）与实际发生之事（世界的回应）之间的空间。每个节拍、场景和序列都必须有鸿沟。无鸿沟 = 无故事；世界在做人物所期待的事。

**何时使用**：任何节拍或场景感觉迟滞、无惊喜或叙事平淡时。

---

### `/mck-image-thread`（意象系统线索）

**用途**：清点意象，审核节奏，验证关键意象的布置，并规定需要添加的意象。
**触发词**：`/mck-image-thread`，"意象系统"，"这个意象是什么"，"串起这个意象"
**输入**：所有散文文件 + state.json（`image_system` 块）
**输出**：意象清单 + 节奏图 + 关键意象审核 + 规定的添加项

**核心概念**：
- **意象系统**：反复出现并积累意义的具体物体、动作或特质的词汇系统
- **关键意象（Key Image）**：到高潮时已将主控思想纳入其中的那个单一反复意象
- **节奏**：意象应以规律间隔出现（不能在第一幕出现后消失，直到高潮才回归）

**何时使用**：`/story-revise` 第4轮（意象系统）；草稿在主题上感觉分散时。

---

### `/mck-setup-payoff`（铺垫-收束台账）

**用途**：构建铺垫-收束台账；检测悬空铺垫和无根之收束。
**触发词**：`/mck-setup-payoff`，"铺垫收束"，"哪些铺垫还悬着"
**输入**：所有散文文件 + state.json（`setup_payoff_ledger` 块）
**输出**：完整台账（每个元素的状态：已规划/已兑现/悬空/无根）

**悬空铺垫**：在早期场景中植入，从未兑现。
**无根之收束**：在高潮兑现，从未铺垫。

两者对读者而言都像作弊。悬空铺垫像食言；无根之收束像天降神兵。

**何时使用**：`/story-revise` 第4轮；结局感觉没有赢得时；早期细节被遗忘时。

---

### `/mck-specificity-forge`（具体性锻造）

**用途**：扫描通用语言，从世界圣经中锻造具体的特殊性。
**触发词**：`/mck-specificity-forge`，"让这个更具体"，"替换通用词"
**输入**：散文草稿 + `world-bible.md`
**输出**：按优先级排序的具体性台账（严重/主要/次要）+ 具体替换项

**标记的内容**："一个男人"、"一个房间"、"走"、"轻声说"、"看起来悲伤"——描述类别而非特定个体的词。替换必须与世界和人物的视角一致。

**何时使用**：`/story-revise` 第6轮（具体性）；任何段落读起来通泛时。

---

### `/mck-voice-first`（声音优先）

**用途**：在草拟之前锁定声音锚点——定义这个故事散文声音的具体词汇范围、节奏、语域和句法习惯。
**触发词**：`/mck-voice-first`，"锁定声音"，"声音锚点"
**输入**：任何种子散文、作者人格、类型合同
**输出**：`drafts/{slug}/voice-anchors.md`——所有后续散文的校准文件

**锁定的内容**：
- 句子长度分布（长短比例，片段句使用）
- 词汇语域（特定时代或世界的排除项）
- 内省风格（近第三人称/自由间接叙事/第一人称）
- 对话风格（简练/饶舌/仪式性）
- 体现这种声音的具体句子

**何时使用**：第一个散文场景写作之前；多会话项目中检测到声音漂移时。

---

### `/mck-exposition-ammo`（铺陈即弹药）

**用途**：将背景交代和信息堆积转化为"铺陈即弹药"——每个事实都在某人在战斗中揭露、隐瞒或武器化它的场景中被发射。
**触发词**：`/mck-exposition-ammo`，"铺陈太多"，"信息堆积"，"背景场景"
**输入**：含铺陈的散文 + 场景卡

**核心原则**：铺陈不是被"传递"的——而是被"争夺"的。受众所需的每一条信息，都被置于一个场景中，其中某人对信息的揭露、隐瞒或扭曲有所关切。

**何时使用**：任何场景主要由角色互相解释事情时；背景以旁白形式传递时。

---

### `/mck-negation-of-negation`（否定之否定）

**用途**：将中心价值驱向第四角——正值的腐化——以测试危机是否具有真正的深度。
**触发词**：`/mck-negation-of-negation`，"否定之否定"，"深化危机"

**四个角**：
- 第一角：正值（爱、正义、自由）
- 第二角：对立（漠然、不公、禁锢）
- 第三角：否定（仇恨、腐败、奴役）
- 第四角：否定之否定（毁灭他人的爱、不公正的正义、奴役自身的自由）

第四角的危机代价远高于第三角，也更为真实。

**何时使用**：危机感觉太容易时；对手感觉像反派而非一种论点时。

---

## 9. 基础设施技能参考手册

这些技能作用于平台本身，而非故事内容。

---

### `/story-persona`（作者人格）

**用途**：锻造、加载或应用作者人格——"写了"这个故事的那个具体的虚构作者意识。
**触发词**：`/story-persona`，"作者人格"，"锻造一个人格"，"谁写了这个"
**输出**：`drafts/{slug}/persona.md`

**三种模式**：

**FORGE（锻造）**：提5个问题并推导人格：
1. *创伤知识*——这位作者无需假装便能置身其中的情感领域
2. *主导信念*——关于人性的一句真实而令人不舒服的话
3. *美学取向*——什么样的场景或句子让这位作者向前倾
4. *拒绝*——这位作者出于审美原因（非道德原因）绝不会写的东西
5. *暗面处理方式*——这位作者如何处理暴力、悲伤、羞耻：直视？迂回？温柔？冷静？

**LOAD（加载）**：读取 `persona.md`，压缩为5条工作参考供当前会话使用。如有草稿存在且声音锚点尚为模板，则自动从已提交散文中填写声音锚点。

**APPLY（应用）**：通过4点决策协议过滤具体的审美决定：
1. 信念测试——这个选择戏剧化了主导信念吗？
2. 美学测试——这向这位作者认为美丽的东西靠近了吗？
3. 拒绝测试——这越过了任何审美红线吗？
4. 真相测试——这来自真相图书馆中的条目，还是来自陈词滥调？

**何时使用**：项目启动时 FORGE（由 `/story-new` 自动提供）。`/story-scene` 第2步自动 LOAD。任何审美决定不明确时 APPLY。

---

### `/story-stop-loss`（止损协议）

**用途**：收敛协议——通过执行迭代上限、升级路径和回溯深度限制，防止无限修订螺旋。
**触发词**：内部调用（由其他工作流技能调用）；`/story-stop-loss` 可查看协议说明

**上限**：
- 修复循环（成果物未通过断言）：**5轮**
- 质量提升（成果物通过断言）：**3轮**
- 单节拍修订：**3轮**
- 批评者分歧：**2轮**

**三振出局规则**：同一断言在同一成果物上连续3轮失败 → 立即停止，向用户呈现，提供备选方案。

**达到上限时的三条出路**：
- A——手动编辑（用户修复具体失败点）
- B——上游修改（回溯到场景卡/人物/脊椎事件）
- C——放弃并以不同种子重新开始

**何时使用**：自动应用。理解它有助于系统升级时——这意味着当前方法已耗尽修订预算，需要不同策略。

---

### `/story-tournament`（锦标赛生成）

**用途**：高风险创意决策的锦标赛生成——并行生成N个多元候选，盲判，选出赢家。
**触发词**：`/story-tournament`，"运行锦标赛"，"给我选项"，"竞争性高潮设计"

**五场锦标赛**：

| 锦标赛 | 候选数 | 多样性槽位 |
|---|---|---|
| 主控思想 | 3 | 理想主义/悲观主义/讽刺性 |
| 激励事件 | 3 | 外部冲击/自我引发/延迟认知 |
| 主角真实性格 | 3 | 最大矛盾/隐性共谋/倒置创伤 |
| 危机 | 5 | 正vs正/恶vs恶/自我vs他者/欲望vs需求/价值vs价值 |
| 高潮 | 5 | 正向/负向/讽刺性/内省最大化/行为性 |

**锦标赛后**：跨锦标赛一致性检查——独立选出的成果物能否协同工作？（主控思想↔主角/激励事件↔危机/危机↔高潮）

**失败者归档**：落败候选存储在 `drafts/{slug}/tournament-archive/`——如赢家后来需要修改，这些可用。

**何时使用**：当某个高风险决策的默认答案感觉就是"显而易见的选择"时。显而易见的选择通常不是最好的。

---

### `/mck-honesty`（诚实引擎）

**用途**：通过诚实引擎测试、施压和修复主控思想——将扎根于真实的想法与道德口号区分开来。
**触发词**：`/mck-honesty`，"测试主控思想"，"这个主题是真实的吗"，"诚实性检查"

**三种模式**：

**TEST（测试）**：三项检查：
1. *信念检查*——作者有来自（真相图书馆的）证据证明这个说法是真实的吗？评分：扎根/可信/断言
2. *结构检查*——三个子测试：去对话测试（剥去所有对话后，主控思想还能从事件中浮现吗？）；信念反转测试（用相反的信念替换主角的表述信念，故事还能抵达同样的主控思想吗？）；对手即对立面测试（对手在行动中而非言语中体现了对立思想吗？）
3. *对立面检查*——对立思想获得了真正的战斗，还是稻草人？

**STRESS（施压）**：生成针对主控思想的最强可能反驳。识别反驳与故事对手设计之间的差距。

**REPAIR（修复）**：诊断哪项检查失败，并提出两个有具体脊椎或对手修改方案的扎根替代方案。

**何时使用**：主控思想FORGE之后（自动提供）；`/story-spine` 开始前；`/story-audit` 第1轮（自动）。

---

### `/mck-surprise-plant`（惊喜植入）

**用途**：设计和审核必然性惊喜架构——让高潮既令人惊喜又令人感到"本应如此"的双重解读系统。
**触发词**：`/mck-surprise-plant`，"必然性惊喜"，"植入伏笔"，"误导计划"

**双重解读规则**：每个植入项必须同时通过两个测试：
- *表面测试*（初读）：在读者当时所知的情况下，支持被误导的期望
- *真相解读测试*（重读）：清楚地指向实际结局，但在误导下未被注意

**三种模式**：

**DESIGN（设计）**：从已锁定的高潮向后推导。定义被误导的期望 vs 真实结局。清点6—10个双重解读候选。选择4—6个，写入 `misdirection-plan.md`。必须在第一幕写作之前完成——不能事后嵌入。

**PLANT（植入）**：将每个项目分配到具体的场景卡。指定精确的植入动作及读者不会察觉的原因。验证跨第一幕和第二幕的分布。

**AUDIT（审核）**：召唤 `surprise-auditor` 代理进行盲读 + 交叉比对。返回 `surprise-audit.md`（通过/需修复/结构性失败 的裁定）。

**何时使用**：DESIGN 在 `/story-spine` 锁定后自动提供。PLANT 在幕规划期间。AUDIT 在完整散文草稿完成后。

---

## 10. 智能代理参考手册

代理由技能（通常在内部）为需要上下文隔离或并行处理的有界任务召唤。你也可以通过对话中的 `Agent` 工具直接调用。

### 生成代理

这些代理产出成果物。

| 代理 | 输入 | 输出 | 由谁召唤 |
|---|---|---|---|
| `premise-prospector` | 萦绕 + 约束 | 5候选前提候选 | `/story-new` |
| `character-forger` | 前提+类型+设定 | 人物文件（人物塑造/真实性格/维度/传记） | `/story-cast` |
| `structure-skeleton` | 前提+主控思想+类型+人物 | 含美人鱼时间线的脊椎文件 | `/story-spine` |
| `scene-architect` | 脊椎+幕序列+人物 | 每个场景的场景卡 | `/story-act` |
| `beat-miner` | 场景卡 | 节拍表（编号行动/反应单元、鸿沟分析、转折点） | `/story-scene`（无节拍表时） |
| `prose-drafter` | 节拍表+人物文件+声音锚点 | 场景的完整散文草稿 | 可选，作为上下文内草拟的替代 |

### 批评代理

这些代理盲读草稿并返回发现。冷启动是特性——它们没有作者偏见。

> **跨平台**：批评代理是 `/story-audit` 与 `/story-scene` 所用能力阶梯的最高一档。在没有代理召唤能力的宿主上（OpenCode、Pi 等），每个批评维度会降级到可用的原生批评工具（如 Pi 的 `cliche_hunt`、`subtext_check`、`antagonism_test`、`pacing_analyze`、`reader_simulate`、`setup_payoff`），或降级到带"换新眼"重置的上下文串行。各档位的维度与提问完全相同；见 `/story-audit` 内的跨平台批评映射表。纯上下文模式更弱（单一视角、自审偏差），所选档位会记入审核报告。

| 代理 | 审核什么 | 由谁召唤 |
|---|---|---|
| `antagonism-stress-tester` | 力量平衡：内层/人际/超个人层面的对抗力量是否足以让胜利代价高昂？ | `/story-audit` |
| `crisis-climax-auditor` | 两难困境真实性；高潮因果性；主要戏剧性问题是否被回答；主控思想是否在价值最终转变时得到戏剧化 | `/story-audit`，`/story-spine` |
| `cliche-hunter` | 套话、套图、套路人物、套招；区分于被尊重的类型惯例 | `/story-audit`，`/story-scene` |
| `subtext-whisperer` | 文本≈欲望（直白）；角色在说他们的真实意思 | `/story-audit`，`/story-scene` |
| `composition-conductor` | 铺垫-收束台账；意象系统节奏；节奏和节奏分布；过渡 | `/story-audit` |
| `continuity-supervisor` | 世界规则违反；人物知识的时代错误；物理上的不可能；时间线错误 | `/story-scene` |
| `voice-drift-detector` | 针对 `voice-anchors.md` 的声音不一致；时代错误词汇；语域漂移 | `/story-revise` 第5轮 |
| `specificity-auditor` | 通用名词和动词；按优先级排序的台账（严重/主要/次要）；世界圣经感知的替换 | `/story-revise` 第6轮 |
| `reader-simulator` | 盲读：投入度曲线、困惑点、兴趣跌落处 | `/story-revise` 第7轮 |
| `pacing-analyst` | 场景长度分布；节奏变化；递减回报法则违反 | `/story-revise` 第7轮 |
| `surprise-auditor` | 误导完整性；每个植入项的双重解读可用性；高潮处的重读时刻 | `/mck-surprise-plant AUDIT`，`/story-audit` |
| `tournament-judge` | 针对麦基标准对N个候选进行盲排；返回排名列表+理由+宣布赢家 | `/story-tournament`，多处 |

### 专家代理

| 代理 | 用途 | 由谁召唤 |
|---|---|---|
| `cast-balancer` | 完整人物阵容压力矩阵；冗余诊断；合并/删除/提升建议 | `/story-cast` |
| `genre-cartographer` | 识别类型+子类型；制作类型合同（惯例、必备场景、典范作品） | `/story-new`（可选） |
| `setting-surveyor` | 四维设定（时代/持续时间/地点/冲突层级）；世界规则；研究目标 | `/story-new`（可选） |
| `act-designer` | 幕结构、序列节奏、幕收尾转折点 | `/story-act` |
| `arc-tracer` | 跨脊椎的人物弧光——启示节点、价值走势图、必备启示场景 | 独立或 `/story-cast` |
| `exposition-smuggler` | 将铺陈堆积转化为"铺陈即弹药"的场景改写 | 独立 |
| `key-image-curator` | 关键意象候选；意象系统规则；第一幕（隐晦）+ 高潮（共鸣）的布置计划 | 独立 |

---

## 11. 模板参考手册

### `lifecycle.json`（生命周期文件）

跟踪项目状态。在每个生命周期关卡之后由工作流技能更新。

关键字段：
- `slug` — 项目标识符，用于所有文件路径
- `state` — 当前生命周期阶段（见第5节）
- `locked` — 已完成关卡的布尔值映射（现包含 `published`）
- `target_total` — 计划的总长度（字数/CJK 字符），在幕设计时设定；长度预算关卡据此核对
- `manuscript_built_at` — 上一次 `/story-publish` 组装的时间戳；`/story-status` 据此与散文修改时间对比，检测稿件过期
- `artifacts` — 关键文件的路径（主控思想、脊椎、人格、误导计划等）

### `state.json`（状态数据库）

跟踪场景层面的连续性状态。在每个已提交场景后由 `/story-scene` 第9步更新。在每次场景草拟前由 `continuity-supervisor` 查询。

关键块：
- `characters` — 每个人物：位置、知识事实、物品、关系、创伤状态、当前欲望、价值取向、弧光进展
- `image_system` — 每个意象：引入场景、复现记录、计划中的收束/已兑现收束、关键意象标记
- `setup_payoff_ledger` — 每个元素：铺垫场景、计划收束场景、已兑现状态
- `world_state` — 每个地点：物理状态、最后修改场景；**地理/专名锁**也存于此（楼层数、距离、哪栋楼是哪栋），使场景卡与散文无法自相矛盾
- `exposition_ledger` — 每个事实：传递方式（隐瞒/提取/武器化/强制揭露）、计划场景、已传递状态
- `timeline` — 故事开始日期、场景时间戳

在 `/story-act` 第7步搭建（不再可选）。缺失时 `/story-scene` 以降级模式运行——连续性追踪、意象系统节奏、铺垫-收束检查均被跳过。

### `persona.md`（作者人格文档）

由 `/story-persona FORGE` 写入。

关键部分：
- 作者名字
- 作者的亲历知识（创伤知识）
- 作者的主导信念
- 作者认为美丽的东西
- 作者的拒绝（审美红线）
- 形式习惯（5个轴向选择）
- 作者处理暗面的方式
- 作者与类型的关系
- 真相图书馆（3—5条具体的人类行为观察）
- 声音锚点（从已提交散文中填写）
- 决策协议（4过滤器：信念/美学/拒绝/真相）

---

## 12. 完整工作流演示

从种子到锁定脊椎的完整示例，展示技能调用序列及其输出。

**种子**："一位宗门长老多年前被驱逐的弟子归来，用宗门视为异端的方法变得更强。"

---

### 第一次会话 — 奠基

```
/story-new "一位宗门长老多年前被驱逐的弟子归来，用宗门视为异端的方法变得更强。"
```

技能问："这个种子有什么让你放不下？"

你回答："一个人用证明方法正确的方式，同时证明了自己是错的。"

技能提炼萦绕："一个赢得了辩论但在赢得辩论的过程中失去了自己的人。"

项目代号生成：`reverse-dao`

故事新建提供作者人格锻造：

```
/story-persona FORGE
```

回答5个问题。技能综合出：主导信念："原则的证明与原则本身是同一件事——你无法通过违背真理来证明真理。"真相图书馆：3条具体观察。形式习惯锁定。

前提候选生成（5个）。你选择第2个。

```
lifecycle.json → state: "premise_locked"
```

---

### 第二次会话 — 主控思想与脊椎

```
/controlling-idea-architect FORGE
```

生成3个候选（理想主义/悲观主义/讽刺性）。你选择讽刺性的："精通摧毁容纳它的人，因为那个人用精通替代了值得精通的一切。"

诚实引擎自动运行：信念检查（扎根——映射到真相图书馆："将自己奉献给某个理想的人，往往用对理想的奉献取代了理想本身"）。结构检查：对立思想（宗门的立场：纪律赋予合法性）是一场真正的战斗。通过。

```
controlling-idea.md 写入
lifecycle.json → state: "controlling_idea_locked"
```

```
/story-spine
```

`structure-skeleton` 代理返回脊椎。审核发现：危机是艰难选择（主角必须揭露该方法或保护宗门）。`/mck-crisis-dilemma` 运行强化。

新危机：主角必须在以下二者之间选择：销毁该方法的证据（维护宗门的合法性，但发现就此消失）；或公开它（验证异端，摧毁宗门的权威，证明宗门是错的——正是主角想要的——但代价是主角作为弟子的地位）。

这是欲望vs需求的两难困境。脊椎通过所有断言。

```
lifecycle.json → state: "spine_locked"
```

必然性惊喜架构设计（提供后接受）：

```
/mck-surprise-plant DESIGN
```

被误导的期望：受众期望主角与宗门和解。真实结局：主角实现了和解，发现其代价超过了流放。6个双重解读项目植入第一幕和第二幕。

---

### 第三次会话 — 人物阵容与第一幕

```
/story-cast
```

`character-forger` 为以下人物制作文件：主角、长老、小师弟（留下来的宗门成员）、师姐。`cast-balancer` 发现小师弟和主角压力重叠（都是"忠诚的代价"）——建议区分：小师弟代表被奖励的忠诚；主角代表被惩罚的忠诚。压力现在是独特的。

```
/story-act 1
```

`act-designer` + `scene-architect` 为第一幕制作4张场景卡。所有场景通过"场景转折"断言。

---

### 第四次会话 — 第一个场景

```
/story-scene 1.1
```

五层潜文本表：

| 层级 | 主角 | 守门长老 |
|---|---|---|
| 创伤 | 未经听审被驱逐 | 奉命驱逐而不明其由 |
| 欲望 | 不解释地通过 | 不引发事端地尽职 |
| 恐惧 | 归来毫无意义 | 他驱逐的那件事是对的 |
| 战术 | 贬低（让归来看起来平常） | 拖延（让归来成为别人的问题） |
| 文本策略 | 说话像从短途旅行回来 | 用程序性语言说话 |

鸿沟：主角期望被拒绝（准备好了一场战斗）。守门长老不发一言打开了门走了。鸿沟：*没有阻力*。场景从紧张转向恐惧。

批评代理审核：`cliche-hunter` 标记套话——用具体的行为细节替换。`subtext-whisperer` 发现一行主角说出了他真正想要的——改写。`continuity-supervisor`：无状态违反。

提交后：意象系统更新（"石阶"意象首次出现）。铺垫-收束台账：新铺垫注册（"守门长老开门时的具体手势"）。

---

## 13. 进阶功能：V3

V3功能适用于任何项目。需要先锻造人格（`persona.md` 必须存在，诚实引擎和人格过滤锦标赛才能运行）。

### 作者人格作为决策过滤器

`/story-scene` 第5步中，每个节拍都通过作者人格决策协议静默过滤。只有失败时才浮出水面。示例：

*节拍*：主角进入长老的书房；长老没有从写作中抬头。

*人格过滤器静默运行*：
- 信念测试：这个选择戏剧化了"原则的证明与原则本身是同一件事"吗？长老的不回应是长老把主角视为仍未得证。✅
- 美学测试：这向"慷慨姿态被扣留的时刻"靠近了吗？是的——这正是姿态的拒绝。✅
- 拒绝测试：这越过了任何审美红线吗？没有。✅
- 真相测试：映射到真相图书馆："对自己立场最确信的人，通常最怕被要求为之辩护。"长老的沉默是作为盔甲的确信。✅

节拍通过。展示给用户。

### 高潮设计锦标赛

```
/story-tournament CLIMAX
```

生成5个高潮设计，跨越：正向/负向/讽刺性/内省最大化/行为性。`crisis-climax-auditor` 对每个运行。`tournament-judge` 盲排。跨锦标赛一致性检查：选出的高潮是否来自之前选出的危机？

### 中途进行惊喜架构

如果你在脊椎锁定时没有运行 `/mck-surprise-plant DESIGN`，仍可在第一幕散文写作之前运行（DESIGN模式）。如果散文已经存在：对尚未草拟的场景卡运行PLANT，对现有散文运行AUDIT以找出泄露点。

---

## 14. 故障排查

### "场景在3轮后仍无法通过潜文本断言"

止损协议已触发三振出局升级。原因几乎总是在上游：

1. 检查五层潜文本表——人物的**战术**是否真的是"对他人施加的动词"？（"感到矛盾"不是战术；"羞辱"才是。）
2. 检查人物的欲望——它是否太笼统？（"被理解"不可执行；"逼迫道歉"可以。）
3. 如果两者都看起来正确：场景卡的目标可能与人物的欲望不对齐。场景有两套议程在相互较劲。

运行 `/story-scene` 回溯协议：命名需要修改的具体上游字段，得到确认，应用修改，重新开始。

### "危机-高潮审核员一直无法通过高潮"

高潮因果地来自危机决策——如果一直失败，高潮可能不是由决策引起的。测试：如果主角做了*相反的*危机决策，高潮会有所不同吗？如果没有：高潮不是决策的后果，而是一个独立事件。

修复：追踪相反决策会产生什么，确保所选决策导致明显不同的结果。

### "前提候选感觉通泛——5个选项都是同一个故事的变体"

`premise-prospector` 代理从萦绕生成。如果萦绕陈述得太抽象（"一个关于救赎的故事"），候选项都会是抽象的。用具体的图像、关系或情境重新陈述萦绕："一个人发现他写了但从未寄出的信，现在在另一个人手里。"

### "story-audit 通过了，但故事感觉空洞"

所有结构断言通过不保证质量——只保证结构合规性。空洞感是一个V3问题：

1. 运行 `/mck-honesty TEST`——主控思想可能通过了7点审核，但没通过扎根检查。如果是"断言"：故事是围绕一句口号建立的。
2. 检查 `persona.md` 中的真相图书馆——场景是在汲取具体的真相，还是在使用类型模板？
3. 运行 `/mck-specificity-forge`——通用语言让情感内容变得不可见。

### "人格感觉是强加的——通过决策协议的节拍仍感觉不对"

人格的主导信念可能太笼统。"人摧毁他们爱的东西"是真实的，但太宽泛；"最需要被原谅的人最无法开口求原谅"具体到可以过滤真实的审美决策。

重新运行 `/story-persona FORGE`。更具体地回答信念问题。一个具体到可以被一个特定事件序列证伪的信念，才足够具体地过滤真实的审美决策。

### "惊喜架构感觉像把戏，而非必然性惊喜"

`surprise-auditor` 代理区分*事后欺骗*和*双重解读可用*。事后欺骗是指仅在高潮命名它为"有意义"之后才作为伏笔起作用的项目。双重解读是指受众在重读时无需叙事指引便能看出其真实意义的项目。

审核你的植入项：每一个在初读时是否**主动**支持表面解读？如果某个项目需要读者粗心或好奇心不足才能错过真实解读：那是欺骗。要么让表面解读更有说服力，要么用一个真正支持双重解读的项目替换它。

### "我所在的宿主没有 Agent 工具——怎么跑审核？"

批评套件会优雅降级。`/story-audit` 第 0 步（能力阶梯）会选择宿主支持的最高一档：宿主提供时用原生批评工具（如 Pi 的 `cliche_hunt`/`subtext_check`），否则用带"换新眼"维度重置的上下文串行。审核照样运行、照样落盘报告；只是纯上下文模式更弱（单一视角、自审偏差）。把所选档位记入报告抬头，并把自己的散文比导入的散文审得更严。见 `/story-audit` 内的跨平台批评映射表。

### "审核跑到一半挂了（会话/限流）——整轮都白跑了吗？"

不会。每个批评在返回摘要*之前*先写入 `drafts/{slug}/audit/{critic}.md`。重启后读取已落盘的报告，仅重跑缺失的批评——绝不静默丢弃任何一个。（正因如此，5 个批评跑完 4 个后撞上限额的那一轮是可恢复的。）

### "成稿远短于计划"

这是最常见的隐性失败，现在应能尽早发现。`act-design.md` 记录 `target_total` 与各幕预算；每张场景卡有 `length_budget`；`/story-act` 第 6.5 步与 `/story-audit` 会跑**密度检查**。若草稿仍然字数不足，应通过 `/story-scene` 扩写承重幕（最深的那一幕与危机/高潮/结局区——*而非*铺垫幕），再重新生成稿件。警惕**密度倒挂**（铺垫幕最肥、高潮最瘦）——向承重场景重新配比。

### "创作笔记泄漏进了稿件"

`Seq N.N` 引用、`本拍`/强度标签、`✗ 已证伪` 信念清单、拉丁年龄消歧标记（`七十二岁 calendar`）应放在 frontmatter 或末尾的 `<!-- AUTHORING -->` 围栏里——绝不内联在散文正文中（约定见 `/story-scene` 第 9A 步）。泄漏扫描在场景提交时与付梓组装前各跑一次。若泄漏的标记承载读者所需信息（如年龄体系区分），应在*起草时*就定下其面向读者的形态——别拖到付梓，否则会变成最后一刻的世界观决策。

### "我在付梓后改了散文，稿件过期了"

意料之中。付梓后的改动会把项目重新打开到 `polished`。`/story-status` 会检测稿件过期（散文比 `manuscript_built_at` 更新）并标记；重跑 `/story-publish` 重新生成稿件、版权页字数与备注。`done` 不是终点。

---

## 附录：完整技能索引

| 技能 | 版本 | 类型 | 主要输出 |
|---|---|---|---|
| `/story-new` | V1 | 工作流 | premise-card.md + 项目结构 |
| `/story-status` | V1 | 工作流 | 状态报告 |
| `/story-premise` | V1 | 工作流 | premise-card.md |
| `/story-spine` | V1 | 工作流 | spine.md |
| `/story-cast` | V1 | 工作流 | 人物文件 + cast-design.md |
| `/story-act` | V1 | 工作流 | 场景卡 |
| `/story-scene` | V1 | 工作流 | 散文文件 |
| `/story-audit` | V1 | 工作流 | audit-report.md |
| `/story-revise` | V1 | 工作流 | 修订散文文件 + revision-log.md |
| `/story-publish` | V1 | 工作流 | manuscript.md |
| `/mck-subtext-5layer` | V1 | 方法论 | 五层表 + 改写节拍 |
| `/mck-controlling-idea` | V1 | 方法论 | 主控思想候选 |
| `/mck-beat-to-prose` | V1 | 方法论 | 从节拍表生成的散文草稿 |
| `/mck-crisis-dilemma` | V1 | 方法论 | 强化后的危机设计 |
| `/mck-arc-walk` | V1 | 方法论 | 弧光表 + 启示节点 |
| `/mck-gap-find` | V1 | 方法论 | 每节拍/场景的鸿沟分析 |
| `/mck-image-thread` | V2 | 方法论 | 意象清单 + 节奏图 |
| `/mck-setup-payoff` | V2 | 方法论 | 铺垫-收束台账 |
| `/mck-specificity-forge` | V2 | 方法论 | 具体性台账 + 替换项 |
| `/mck-voice-first` | V2 | 方法论 | voice-anchors.md |
| `/mck-exposition-ammo` | V2 | 方法论 | 铺陈即弹药改写 |
| `/mck-negation-of-negation` | V2 | 方法论 | 第四角分析 |
| `/mck-honesty` | V3 | 方法论 | 主控思想扎根裁定 + 修复选项 |
| `/mck-surprise-plant` | V3 | 方法论 | misdirection-plan.md + 场景卡植入 |
| `/story-persona` | V3 | 基础设施 | persona.md |
| `/story-tournament` | V3 | 基础设施 | 候选排名 + 赢家 |
| `/story-stop-loss` | V1 | 基础设施 | 收敛协议（内部） |
| `/controlling-idea-architect` | V1 | 重构技能 | controlling-idea.md |
| `/arc-tracer` | V1 | 重构技能 | 人物弧光文件 |
| `/act-designer` | V1 | 重构技能 | 幕结构文件 |
| `/exposition-smuggler` | V2 | 重构技能 | exposition-ledger.md + 改写 |
| `/key-image-curator` | V2 | 重构技能 | key-image.md |
| `/composition-conductor` | V2 | 重构技能 | composition-audit.md |
| `/wiki-librarian` | V1 | 重构技能 | wiki页面（收录/检查/迁移/重生成） |
